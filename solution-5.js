/* 
5. Write a function that takes a number and returns its square.
*/

function make_square(number) {
    const square = number * number;
    return square;
}

const result = make_square(5);
console.log(result);